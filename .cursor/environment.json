{
  "name": "JobCCQ",
  "install": "npm install && npm run -w @jobccq/api setup && npm run seed",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:api",
      "description": "API Fastify (@jobccq/api) sur http://localhost:4000 en mode watch."
    },
    {
      "name": "web",
      "command": "NEXT_PUBLIC_API_URL=http://localhost:4000 npm run dev:web",
      "description": "Site Next.js (@jobccq/web) sur http://localhost:3000, branché sur l'API locale."
    }
  ],
  "ports": [
    { "name": "web", "port": 3000 },
    { "name": "api", "port": 4000 }
  ]
}
