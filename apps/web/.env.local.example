# URL de l'API JobCCQ (back-end Fastify).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Sentry (idée 112) — vide = pas d'envoi. Même DSN projet ou un projet « web ».
# NEXT_PUBLIC_SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<projet>
